export type Styles = {
  information: string;
  information__total: string;
  paginator: string;
  tableControls: string;
};

export type ClassNames = keyof Styles;

declare const styles: Styles;

export default styles;
